class Account
{
    constructor(id, userID, title, URL, username, password)
    {
        this.id = id;
        this.userID = userID
        this.title = title;
        this.URL = URL;
        this.username = username;
        this.password = password;
    }
}

export default Account;